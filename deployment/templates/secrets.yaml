{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ovp-ms-template-template-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: cluster-secret-store
  target:
    name: ovp-ms-template-template-secrets
    deletionPolicy: Retain
  dataFrom:
  - extract:
      key: /{{ .Values.global.environment }}/ovp-ms-template/template-secrets
  refreshInterval: 5m
{{- else }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: ovp-ms-template-template-secrets
data: {}
{{- end }}
---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: ovp-ms-template-template-env-vars
data:
  {{- range $key, $value := .Values.environments }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
